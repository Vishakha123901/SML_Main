# Copy to .env (for local Netlify dev) or set these in Netlify dashboard > Site settings > Environment
# Instagram Business/Creator or Basic Display long-lived token and user id
INSTAGRAM_USER_ID=
INSTAGRAM_ACCESS_TOKEN=
# Optional: switch to Business API host if needed
# INSTAGRAM_GRAPH_DOMAIN=https://graph.facebook.com

# Frontend can override API path if not using Netlify dev (not recommended)
# VITE_INSTAGRAM_API_URL=/api/instagram
